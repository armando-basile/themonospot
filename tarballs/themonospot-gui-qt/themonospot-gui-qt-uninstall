	rm -rf "$(DESTDIR)$(prefix)/share/applications/themonospot-qt.desktop"
	rm -rf "$(DESTDIR)$(prefix)/share/pixmaps/themonospot-qt.png"
	cd "$(DESTDIR)$(prefix)"
	if test `ls "$(DESTDIR)$(programfilesdir)"| wc -w` -gt 0 ; then \
	    echo "nothing to do"; \
	else \
	    rm -rf "$(DESTDIR)$(programfilesdir)"; \
	    echo "rm -rf '$(DESTDIR)$(programfilesdir)'"; \
	fi